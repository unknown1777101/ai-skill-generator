# Sample React Plugin

An example Antigravity Plugin containing modular skills for React application development.

## Installation
Add this plugin to your project's `.agents/skills.json`:

```json
{
  "entries": [
    { "path": "path/to/plugins/sample-react-plugin/skills" }
  ]
}
```
